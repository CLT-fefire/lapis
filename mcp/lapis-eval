#!/bin/sh
# lapis-eval — 검색 품질(R@1·R@10·MRR·결과수)과 질의 지연 계측.
#
# 공용 러너(`scripts/bundle-run.sh`)가 번들·node 탐색·임시 파일을 맡는다.
set -e
HERE=$(cd "$(dirname "$0")" && pwd)
exec "$(dirname "$HERE")/scripts/bundle-run.sh" eval "$HERE/evalRun.ts" "$@"
