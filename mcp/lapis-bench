#!/bin/sh
# lapis-bench — 인덱스 빌드 비용 벤치.
#
# 공용 러너(`scripts/bundle-run.sh`)가 번들·node 탐색·임시 파일을 맡는다.
set -e
HERE=$(cd "$(dirname "$0")" && pwd)
exec "$(dirname "$HERE")/scripts/bundle-run.sh" bench "$HERE/benchRun.ts" "$@"
