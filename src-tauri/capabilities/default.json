{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "모든 창에 적용. main = 시동 창, w2/w3… = new_window가 만든 보조 창(라벨은 빈 번호 재사용). 여기서 main만 남기면 새 창은 권한이 0이라 dialog·opener가 런타임에 조용히 거부된다.",
  "windows": ["main", "w*"],
  "permissions": [
    "core:default",
    "opener:default",
    "opener:allow-open-url",
    "opener:allow-reveal-item-in-dir",
    "dialog:default",
    "dialog:allow-open"
  ]
}
