# macOS
.DS_Store
*.swp

# Editor
.idea/
*.iml
# .vscode/ 는 추적함 (extensions.json 권장 설정 공유)

# Node / Web
node_modules/
dist/
build/
.cache/
.parcel-cache/

# SvelteKit / Vite
/.svelte-kit
/package
vite.config.js.timestamp-*
vite.config.ts.timestamp-*

# Rust / Tauri
src-tauri/target/
target/
# Cargo.lock — Tauri 앱은 추적 권장 (재현성)

# 환경 변수
.env
.env.local
.env.*.local
!.env.example

# 사용자별 vault (테스트용)
test-vault/
sample-vault/
*.lapis-cache/

# 로그
*.log
logs/

# 개인 바이브 코딩 셋업 (commit 안 함, 본인만 사용)
# - CLAUDE.md: AI 에이전트 행동 강령
# - docs/: 모든 프로젝트 문서 (PLAN/STATE/plans/brainstorms/solutions/adr/diagrams/todos)
#   본인 로컬에서만 작업·관리. GitHub에는 README.md만 노출.
/CLAUDE.md
/docs/
/.claude/
